_base_: ../../blender_materials_n8_w3_aggrprune_long_v7_widerpseudo_x45k.yaml

experiment:
  name: ablation_materials_A3_weak_pvd

# Ablation A3（materials 平行实验）：
# 在 materials v7 x45k 之上只弱化并推迟 PVD，其它（random_bg=off、densify 不收紧、wide_pseudo 与 v7 一致）不变。
# 用来回答：弱 PVD 在 materials（金属高反射场景）上单独能带多少 PSNR 提升？

ssl:
  pvd:
    weight: 5.0e-3      # v7=1.2e-2 -> 5e-3
    start_iter: 9600    # v7=4800   -> 9600
